# Payloads are raw gzip bytes and icons are raw PNG: never let git treat them as
# text, re-encode them, or apply EOL conversion. The manifest in index.html refers
# to these files by content hash, so a single mangled byte breaks the site.
*.gz binary
*.png binary
